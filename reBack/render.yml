services:
  - type: web
    # Keep the Render service name aligned with reFront/config.js.
    # Render derives the public onrender.com hostname from this name.
    name: receipt-analysis-b8po
    runtime: docker
    rootDir: reBack
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APP_FRONTEND_ORIGIN
        value: https://sr5msym6xbw3oil22im7.github.io
      - key: DB_HOST
        fromDatabase:
          name: receipt-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: receipt-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: receipt-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: receipt-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: receipt-db
          property: password

databases:
  - name: receipt-db
    plan: free
    postgresMajorVersion: "18"
